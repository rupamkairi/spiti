I want you to create a directory structure UI for the folders & items of the project.

App should open at the /folders route . where I can see all the folders folders should also contain the items which you need to find a correct way to nest them. A simpler implementation is in .
A Create button should be there to create a new folder or item.
Inside a folder I should be able to create item & see all items that belong there. for this if you need to update the state management do that.

under convex/functions you should be able to find queries for the convex backend that you can reuse.
